import React from "react";
import AuthForm from "../../organisms/auth-form/auth-form.organism";

export const AuthTemplate: React.FC = () => (
  <div className="min-h-screen flex items-center justify-center">
    <AuthForm />
  </div>
);
